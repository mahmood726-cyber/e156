# CLAUDE.md

See **[AGENTS.md](AGENTS.md)** for the canonical rules in this repo — how the
shared `rewrite-workbook.txt` syncs across devices and cloud agents, the
workbook-protection contract (never touch `YOUR REWRITE`; only the author toggles
`SUBMITTED`), the E156 7-sentence format, and the authorship rule.

Claude Code reads this file automatically as per-directory context. It carries no
rules of its own — they all live in AGENTS.md so every agent (Claude, Codex, …)
follows the same contract. If anything here and AGENTS.md ever conflict,
AGENTS.md wins.
